import Testing
@testable import DesignSystem
import SwiftUI

@Test @MainActor func example() async throws {
//    let view = UIHostingController(rootView: ColorPreview()).view
//    assertSnapshot(matching: view, as: .image(size: view.intrinsicContentSize))
}
